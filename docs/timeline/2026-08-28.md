# 时间线记忆 · 2026-08-28

> 由 aitrace 时间线生成（`get_timeline` 操作意图 + 编辑日志统计），按 `/aitrace` 阶段 7 沉淀。
> 版本 0.6.4 · 13 个录制会话 · 16 个提交 · Claude Code · Windows

## 今日轨迹

一天内完成 aitrace 从改名到自进化的完整闭环：

| 时间 | 提交 | 内容 |
| --- | --- | --- |
| 12:06 | `b9dd0b5` | vibetracer → **aitrace** 改名；Windows AF_UNIX（uds_windows）；项目级 hook/MCP/skill/CLAUDE.md |
| 12:19 | `91c1edd` | 清零全部 clippy 警告（22→0） |
| 12:39 | `8d65ce6` | **修复 enrichment 断链**：correlator 键归一化 + session_id 作 agent 身份 |
| 12:39 | `2107313` | watcher 支持 glob ignore，过滤编辑器 `*.tmp.*` 临时文件 |
| 12:39 | `6c8d567` | skill 改名 `/aitrace-review` → `/aitrace` |
| 12:46 | `595024f` | **修复覆盖安装**：被锁 exe 改名让路（0.6.3） |
| 12:51 | `d41947c` | SKILL 写入二进制版本验证流程（哈希/版本标记） |
| 13:04 | `a4118ea` | **修复 hook↔watcher 竞态**：HOOK_GRACE 宽限窗口 + 端到端回归测试 |
| 13:23 | `07d9098` | **intent 特性**：transcript 父链回溯解析双意图（0.6.4） |
| 13:26 | `a029ac2` | README 特性描述如实化 |
| 13:37 | `50a5fdb` | README / SKILL 中文化，口径统一「Claude Code · Windows · Beta」 |
| 13:45 | `95640b7` | CLAUDE.md 中文化：编译/测试/部署纪律 + 自举四原则 |
| 13:5x | （本提交） | 自进化规则闭环 + 首份时间线记忆 |
| 15:1x | （本轮） | **五原则方法论重构**：MAPE-K + PDCA/kaizen + Reflexion + Goodhart/停机规则研究 → 三层嵌套环架构 + 五条自洽公理（可证伪 / 待办守恒 / 终止 / 分层不阻塞 / 单一来源）；SKILL 增阶段 0 开工取件、DoD 四件套（补"教训标准化"）、变异验证提示 |

## 操作意图时间线（intent 特性上线后，源自 transcript 父链回溯）

- 13:24 `src/daemon/intent_index.rs` +515 —— *"提交 07d9098（0.6.4），daemon 32992 已部署。先把那个 clippy 警告修掉……"*（用户请求：**实现**）
- 13:26 `README.md` +476 —— *"新 MCP server（25608，13:26:06 起自 0.6.4 bin）✅。做 MCP 侧最终验收……"*（用户：**先提交**）
- 13:30 `README.md` +401/-476 —— *"两份主文档改中文并按「Claude Code · Windows · Beta」的现状重写……"*（用户：**整理所有的文档，将SKILL和README……改为中文**）
- 13:30 `.claude/skills/aitrace/SKILL.md` +74 —— *"README 完成。SKILL 改中文（allowed-tools 机器解析保持原样）……"*
- 13:31 `README.md` +2 —— *"补一处真实行为（daemon 检测到其他项目的 .claude/ 时自动注册本地 hook）……"*
- 13:39 `CLAUDE.md` +49 —— *"文档整理完成……"*（同上用户请求）

## 文件修改分析

| 文件 | 角色 | 今日改动 |
| --- | --- | --- |
| `src/daemon/mod.rs` | daemon 主循环 | 键归一化 → handler 抽取 → HOOK_GRACE 宽限 → intent 接线（4 轮演进） |
| `src/daemon/intent_index.rs` | **新增** ~500 行 | transcript 增量索引 + 父链回溯 + 9 个单元测试 |
| `src/hook/send.rs` | 元数据转发 | session_id / tool_use_id / transcript_path 透传 + 4 个测试 |
| `src/daemon/correlation.rs` | 关联器 | canonical key（相对化/正斜杠/Windows 小写）+ 双 intent 字段 |
| `src/watcher/fs_watcher.rs` + `src/config.rs` | 过滤 | glob ignore 规则（`IgnoreRule::compile`） |
| `src/install.rs` | 部署 | rename 兜底覆盖被锁二进制 |
| `src/mcp/handlers.rs` | MCP | timeline / search 暴露 `operation_intent` |
| `tests/integration/daemon_hook_race_test.rs` | **新增** | 2 个真实二进制端到端测试（竞态 + intent） |
| README / SKILL / CLAUDE.md | 文档 | 全部中文化，现状口径统一 |
| `docs/` | 归档 | 4 份调研/设计文档入库 |

测试规模：**341 → 350+**，全部由今日缺陷驱动新增。

## 经验教训 → 代码与 SKILL 改进项

| # | 教训 | 已转化的改进 |
| --- | --- | --- |
| 1 | correlator 两侧键不一致（绝对 vs 相对路径），HashMap 永远 miss | 代码：`correlation_key` 统一 canonical 化 |
| 2 | hook-send 用自身 PID 作 agent_id，产生 21 个假 agent | 代码：session_id 作稳定身份 |
| 3 | `.aitrace/bin` 被常驻 MCP 进程锁住，覆盖安装**静默保留旧件** | 代码：rename 让路；规则：部署流程第 2 条 |
| 4 | daemon 子进程锁 `target\debug\aitrace.exe`，链接 os error 5 | 规则：先停 daemon 再构建（SKILL/CLAUDE.md） |
| 5 | MCP server 不随部署升级，易验收到旧代码 | 规则：serverInfo.version 核查 + 提醒人类重连 |
| 6 | PostToolUse 晚于文件落盘，enrichment 是竞态掷硬币（**用户重启验收抓到**） | 代码：HOOK_GRACE 250ms；测试：端到端竞态回归 |
| 7 | 手写 `format!` 转义数错、解析先于插入等低级错 | 测试：helper 生成 JSON、先入 map 再 walk |
| 8 | 验收本身即测试——每轮重启验收都暴露过新问题 | 流程：验收四步 + 哈希/版本标记成为硬规则 |
| 9 | `git add -A` 把验收草稿 `acceptance-temp.md` 卷进提交（靠事后 diff 审视才发现） | 规则：**patch/diff 自省**进入阶段 7（意图↔diff 一致性、意外文件、重做检测、最小化）——教训催生新规则，即自进化闭环本身 |
| 10 | **日志首战即崩**：daemon.log 上线后第一次真实编辑就把 daemon 打死——`&s[..60]` 字节切片切中文字符 panic（变异验证过的测试套件没抓到，因为夹具全是 ASCII） | 代码：`head()` 字符边界截断 + 中文回归测试；教训：**ASCII-only 夹具是变异验证的盲区，真实数据验收不可替代** |

## 待办

### 已完成（目标轮次 2026-08-28 下午「完美状态」冲刺，`43b3e85` + `db0ab8a`，0.6.5，347 测试）

- [x] ~~仓库行尾混杂（69 CRLF / 46 LF）~~ → **已根治**（`6ae115e`）：`.gitattributes` 强制 `* text=auto eol=lf`，工作区 0/28320 全 LF。*五原则首次完整走练*
- [x] ~~触发机械化~~（`43b3e85`）：SessionStart 注入阶段 0 取件指令；Stop 在"有新编辑且未收尾"时一次性提醒（三重 guard：stop_hook_active / `.last-stage7` marker / fail-open），守终止公理
- [x] ~~变异验证试点~~：注释掉 HOOK_GRACE → 竞态测试红（0.81s 精确失败）→ 还原绿——"测试测试"成立；附带教训：panic 泄漏 daemon 进程锁构建 → `DaemonGuard` 落地
- [x] ~~intent 并行批量丢失（架构级）~~（`db0ab8a`）：intent_index 改**查询时活走链**（tool_use→uuid，parent 晚到也能在下次 refresh 命中）+ 落盘 null 帧进回填队列，resolved 后追加纠正记录，`read_all` 按 id 去重保末条——append-only 语义保持。端到端回归测试覆盖
- [x] ~~失败编辑不可见~~：`tool_response.is_error` 由 hook-send 转发 → daemon 计入 `AgentInfo.failed_attempts`（daemon status 可见）。时间线保持"磁盘真相"，重试/摇摆经计数器可观测
- [x] ~~last-prompt 间歇滞后~~：user_prompt 改由 last-prompt 标记**与** user 原文条目双源供给，文件序后者胜
- [x] ~~daemon 重启基线重置~~：`Recorder::with_baseline` 继承上一会话 file_hashes + 快照读取，首帧不再误标 create，跨会话 diff 可用（本次部署实测继承 6 文件）
- [x] ~~`subscribe_edits` 端到端验收~~：新 e2e 测试（socket 订阅 → 触发编辑 → 断言 edit_notification），7 个 MCP 工具全部有验收
- [x] ~~**daemon 无 debug 日志（用户现场指出）**~~：`--daemon-child` 从未初始化 tracing，所有 warn 石沉大海。现在写 `.aitrace/daemon.log`（DEBUG），关键决策点全布点（会话/基线/hook+意图/宽限窗进出/编辑落盘/回填/停止）

### 仍开放

- [x] ~~无重连机检当前 MCP 连接~~（`e3a035c`）→ `list_sessions` 响应附带 `buildHash`，任意常规调用即可机检正在服务的 MCP 进程，无需重连重新 initialize
- [x] ~~目录事件白等宽限窗~~（`991d934`）→ daemon 跳过目录事件（存在且 is_dir），不再空烧 250ms 宽限；删除路径（不存在）照常放行走 Delete 记录
- [x] ~~daemon.log 滚动策略~~（`991d934`）→ 超 1MB 滚动为 `daemon.log.1`（保留一代）
- [x] ~~回填队列不跨 daemon 重启~~（`991d934`）→ 队列持久化 `backfill.json`（当前会话目录保存，重启后回溯加载并接管文件）；**条目携带来源会话目录，纠正写回来源日志**——自查拦下的设计缺陷：写进新会话日志会因 per-session id 空间在去重时顶掉新会话自己的帧。单元测试覆盖 save/load/接管/空队列清除
- [x] ~~待办守恒自动检查器~~（`c7e29e3`）→ `.claude/scripts/conservation-check.ps1` 统计当日记忆 checkbox 产/消，closed=0 即 exit 2；SKILL 阶段 7 第 5 步接线。当前实况：closed=12 open=6
- [x] ~~守恒检查器按日累计而非按轮~~ → 改用 **git diff 增量法**：阶段 7 更新记忆后、提交前运行，未提交的 checkbox 增量即本轮产消（免去轮次标记；先跑脚本后提交的顺序已写入 SKILL）
- [x] ~~守恒检查接入 Stop hook~~ → `stop-reminder.ps1` 触发提醒时自动运行 conservation-check 并把结果附在提醒文本里——守恒机检不再依赖人记得跑（提醒与检查合一，guard 三重性不变）
- [x] ~~**守恒律停机修正案**（机检逮住的本轮零产出所逼出，公理级发现）~~：消1产1 守恒与"池清空即完美"在稳态下不相容（池大小恒定，不动点不可达）。修正：新增**收尾轮豁免**——当池中仅剩不可立即推进项（需人工/跨会话/设计轮）时，本轮可记产 0，须在记忆中标注"收尾轮"；checker 加 `-AllowNoProduce` 开关对应。已落 CLAUDE.md 公理 2 + SKILL 步骤 5
- [x] ~~目录事件白等宽限窗~~（陈旧行，正式关闭见下方 991d934 条目）：watcher 对目录本身的事件也进 HOOK_GRACE 等 250ms；跳过"enrichment 队列为空"或非文件事件即可
- [x] ~~MCP server 重连~~ → 新进程 41024（14:43 起，0.6.5 构建），timeline 查询验证通过；`read_all` 去重自此对 MCP 查询生效
- [x] ~~SessionStart / Stop hooks 实测~~ → SessionStart 在会话恢复时成功注入阶段 0 取件指令（本会话系统消息可见）；Stop 的 marker 静默路径与 block 路径（脚本级 exit 2）均验证——触发机械化完全上线
- [x] ~~待办守恒自动检查器~~（陈旧行，正式关闭见下方 c7e29e3 条目）（DoD：解析当日记忆 checkbox 产/消统计，阶段 7 调用）：本轮未做，防止人工漏检
- [x] ~~daemon.log 滚动策略~~（陈旧行，正式关闭见下方 991d934 条目）（DoD：按大小截断或按会话分文件）：当前 append 无上限
- [x] ~~回填队列不跨 daemon 重启~~（陈旧行，正式关闭见下方 991d934 条目）：daemon 停止时未回填条目丢失（设计边界，量小可接受，记录在案）
- [ ] TUI Inline Annotations（`A` 键）展示真实 intent 的人工验证（代码路径已存在：`file_view.rs` 注释列渲染 `operation_intent`，待人工目验）——**被 Windows 输入问题阻塞**：自检模式实测 raw mode 生效（0x1f7→0x1f0 清 ECHO/LINE）但 **QUICKEDIT 残留**（鼠标一点即吞掉全部键盘、右键粘贴剪贴板——解释全部症状含 `ddd ttt` 怪串）；`aaa737c` 加 `harden_console_input()` 清 QUICKEDIT/INSERT/MOUSE，部署后待复验
- [ ] **守恒检查器盲区**（收尾提醒实测发现，DoD：脚本加 `--staged` 模式统计 `git diff HEAD` 全仓或至少报告"有未提交变更但记忆文件未动"）：checker 只 diff 记忆文件，代码未提交时误报 clean tree
- [ ] **守恒计账：推进不计**（本轮实测，DoD：checker 识别"行内容变更但状态未变"的 `[ ]` 行计为消费半分）：TUI 待办原地推进（定位+修复）被计 consumed=0 误报
